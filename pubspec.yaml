name: firebase_chat_messenger
description: The package provides chat messenger features like Telegram or KakaoTalk.
version: 0.0.5
author:
homepage:

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^0.4.4+3
  firebase_auth: ^0.16.0
  cloud_firestore: ^0.13.0+1

  provider: ^4.0.5
  sqflite:
  uuid: ^2.0.4
  path: ^1.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
